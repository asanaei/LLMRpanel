% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gui.R
\name{run_panel_studio}
\alias{run_panel_studio}
\title{Launch the LLMRpanel Shiny GUI}
\usage{
run_panel_studio(...)
}
\arguments{
\item{...}{Passed to \code{\link[shiny:runApp]{shiny::runApp()}} (e.g. \code{port}, \code{launch.browser}).}
}
\value{
Invisibly, the value of \code{\link[shiny:runApp]{shiny::runApp()}}; called for the side effect
of starting the app.
}
\description{
Starts a Shiny application that builds a persona panel, administers a choice
item or conjoint instrument, and presents the package's diagnostics and
design analyses. Choice-item response shares can be compared with an
optional benchmark. The application can download the responses and report
in a zip file, with the benchmark table when one is available.
}
\details{
The GUI is optional. It needs the suggested packages \code{shiny}, \code{bslib}, \code{DT},
and \code{LLMR.shiny}; install them first. Keys are read from environment variables
only, never pasted into the app; a deterministic demo mode runs offline.
}
\examples{
if (interactive() &&
    requireNamespace("shiny", quietly = TRUE) &&
    requireNamespace("LLMR.shiny", quietly = TRUE)) {
  run_panel_studio()
}
}
