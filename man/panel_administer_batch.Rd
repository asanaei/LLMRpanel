% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/batch.R
\name{panel_administer_batch}
\alias{panel_administer_batch}
\title{Administer a panel asynchronously through the batch API}
\usage{
panel_administer_batch(panel, instr, config, state_path = NULL)
}
\arguments{
\item{panel}{A \code{\link[=panel_from_margins]{panel_from_margins()}} / \code{\link[=panel_from_data]{panel_from_data()}} /
\code{\link[=panel_from_personas]{panel_from_personas()}} result.}

\item{instr}{A \code{\link[=panel_instrument]{panel_instrument()}}.}

\item{config}{An \code{LLMR::llm_config()} for a generative model on a provider with
a supported batch API (OpenAI, Groq, Anthropic, Gemini).}

\item{state_path}{Optional path; when given the job is also saved there as RDS
so it can be fetched from another session.}
}
\value{
A \code{panel_batch_job} handle.
}
\description{
Submits one request per persona x item to the provider's batch API (about half
the synchronous price, up to a 24-hour turnaround, no rate-limit pressure) and
returns immediately with a job handle. Complete it later with
\code{\link[=panel_administer_fetch]{panel_administer_fetch()}}; check progress with \code{\link[=panel_batch_status]{panel_batch_status()}}. This is
the recommended path for a large panel; the synchronous \code{\link[=panel_administer]{panel_administer()}}
stays the default for small ones.
}
\details{
All personas are administered under one \code{config} (one model). The handle and
its optional \code{state_path} file carry the survey prompts and the rendered
persona text, but no API key value.
}
\examples{
\dontrun{
panel <- panel_from_margins(list(party = c(left = .5, right = .5)), n = 200)
instr <- panel_instrument(item_likert("wk4", "A four-day work week helps."))
cfg <- LLMR::llm_config("groq", "openai/gpt-oss-20b")
job <- panel_administer_batch(panel, instr, cfg, state_path = "panel_job.rds")
# ... later:
resp <- panel_administer_fetch(job)
}
}
\seealso{
\code{\link[=panel_administer_fetch]{panel_administer_fetch()}}, \code{\link[=panel_batch_status]{panel_batch_status()}},
\code{\link[=panel_administer]{panel_administer()}}.
}
