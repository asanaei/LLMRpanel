% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/persona_frame.R
\name{as_persona_frame}
\alias{as_persona_frame}
\title{Attach the persona contract to a data frame}
\usage{
as_persona_frame(data, questions = NULL, demographics = NULL, answers = NULL)
}
\arguments{
\item{data}{A decoded data frame, one respondent per row. Values should
already be human-readable labels (decode a labelled survey file with, for
example, \code{haven::as_factor()} first).}

\item{questions}{Optional named character vector mapping column names to the
human question wording, e.g.
\code{c(pid = "Party identification", ab = "Abortion position")}. Columns absent
from this map keep their column name. Without it the column names stand in
for the questions, which is formatting, not a faithful translation.}

\item{demographics}{Optional character vector of columns to treat as
demographic background (the rest become stated answers). Defaults to the
common demographic names found in \code{data}.}

\item{answers}{Optional character vector restricting which columns may appear
as stated answers. Defaults to every column that is not a demographic, an
\code{id}-named, or a \code{weight}-named column, so analysis-only columns do not leak
into the prompt.}
}
\value{
\code{data} with the persona contract attached and class \code{persona_frame}.
}
\description{
Marks a decoded data frame as a persona source so that
\code{\link[=panel_from_data]{panel_from_data()}} renders each row as a person described by their
demographics and their stated answers, keyed by the survey question wording,
rather than by a flat template. This is how an arbitrary data frame becomes
good text for the model: you supply the column-to-question mapping, and the
rendering is handled by \code{\link[LLMR:llm_persona_split]{LLMR::llm_persona_split()}}.
}
\details{
Turning a frame into a \code{persona_frame} is the only way to opt into rich
rendering; a plain data frame is always rendered by the previous flat
template, so existing panels are unaffected.
}
\examples{
df <- data.frame(
  age = c("35-44", "65+"),
  pid = c("Strong Democrat", "Strong Republican"),
  ab  = c("Always legal", "Never legal"))
pf <- as_persona_frame(
  df,
  questions = c(pid = "Party identification", ab = "Abortion position"),
  demographics = "age")
}
\seealso{
\code{\link[=panel_from_data]{panel_from_data()}}, \code{\link[=panel_from_personas]{panel_from_personas()}},
\code{\link[LLMR:llm_persona_split]{LLMR::llm_persona_split()}}.
}
