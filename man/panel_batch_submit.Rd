% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/batch.R
\name{panel_batch_submit}
\alias{panel_batch_submit}
\title{Administer a panel asynchronously through the batch API}
\usage{
panel_batch_submit(
  panel,
  instrument,
  config,
  state_path = NULL,
  max_calls = 5000L,
  confirm = FALSE
)
}
\arguments{
\item{panel}{A \code{\link[=panel_from_margins]{panel_from_margins()}} / \code{\link[=panel_from_data]{panel_from_data()}} /
\code{\link[=panel_from_personas]{panel_from_personas()}} result.}

\item{instrument}{A \code{\link[=panel_instrument]{panel_instrument()}}.}

\item{config}{An \code{LLMR::llm_config()} for a generative model on a provider with
a supported batch API (OpenAI, Groq, Anthropic, Gemini).}

\item{state_path}{Optional path; when given the job is also saved there as RDS
so it can be fetched from another session.}

\item{max_calls}{Integer. If the run would make more than this many calls, it
stops unless \code{confirm = TRUE}. Default 5000.}

\item{confirm}{Logical. Set \code{TRUE} to proceed past \code{max_calls}.}
}
\value{
A \code{panel_batch_job} handle.
}
\description{
Submits one request per persona and item to a provider's batch API and returns
a job handle. Use \code{\link[=panel_batch_status]{panel_batch_status()}} to inspect the job and
\code{\link[=panel_batch_fetch]{panel_batch_fetch()}} to retrieve completed results. Provider services
determine prices and completion times.
}
\details{
All personas are administered under one \code{config} (one model). The handle
carries the survey prompts and rendered persona text. When \code{state_path} is
supplied, the API key must be referenced through an environment variable so
its value is not written to the saved state.
}
\examples{
\dontrun{
panel <- panel_from_margins(list(party = c(left = .5, right = .5)), n = 200)
instrument <- panel_instrument(item_likert("wk4", "A four-day work week helps."))
cfg <- LLMR::llm_config("groq", "openai/gpt-oss-20b")
job <- panel_batch_submit(panel, instrument, cfg, state_path = "panel_job.rds")
# ... later:
resp <- panel_batch_fetch(job)
}
}
\seealso{
\code{\link[=panel_batch_fetch]{panel_batch_fetch()}}, \code{\link[=panel_batch_status]{panel_batch_status()}},
\code{\link[=panel_administer]{panel_administer()}}.
}
