% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/benchmark.R
\name{panel_power}
\alias{panel_power}
\title{Calculate two-arm sample sizes from panel responses}
\usage{
panel_power(
  responses,
  effect,
  items = NULL,
  focal = NULL,
  alpha = 0.05,
  power = 0.8
)
}
\arguments{
\item{responses}{A \code{\link[=panel_administer]{panel_administer()}} result (the silicon pilot).}

\item{effect}{Raw minimum detectable difference between the two arms:
scale points for Likert items, a difference in proportions for choice
items. A scalar (recycled) or a named vector keyed by \code{item_id}.}

\item{items}{Optional character vector restricting which items to include.}

\item{focal}{Optional named character vector keyed by \code{item_id}, giving the
focal response level whose proportion the power calculation should target.
For a binary choice item the modal option is a well-defined estimand and
\code{focal} is optional; for a choice item with three or more options the
"modal share" is not a meaningful single proportion, so name the focal
response here. Without a \code{focal} for a 3+-option item, the modal share is
used and a warning is issued. A named focal that the pilot never elicited is
still powered: as long as it is one of the item's options, the observed rate
is taken as 0 (with a warning) rather than an error; only a focal that is not
an offered option of the item is rejected.}

\item{alpha}{Two-sided test size.}

\item{power}{Target power.}
}
\value{
A tibble: \code{item_id}, \code{type}, \code{dispersion} (sd for Likert, the focal
or modal share for choice), \code{effect}, \code{n_per_arm}.
}
\description{
Calculates analytic sample sizes for a two-arm study. Likert items use the
standard deviation of \code{score}. Choice items use the share of a focal response
or, when appropriate, the modal response. Open items are omitted.
}
\examples{
\dontrun{
set.seed(110)
panel <- panel_from_margins(list(group = c(A = .5, B = .5)), n = 8)
instrument <- panel_instrument(list(
  item_likert("lik", "Rate the proposal.", scale = c("low", "mid", "high")),
  item_choice("pick", "Pick one.", c("A", "B"))),
  randomize = character(0))
cfg <- LLMR::llm_config("groq", "openai/gpt-oss-20b")
r <- panel_administer(panel, instrument, cfg)
panel_power(r, effect = c(lik = 0.5, pick = 0.2))
}
}
